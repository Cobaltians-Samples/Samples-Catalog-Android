<resources>

    <!-- Base application theme -->
    <style name="AppTheme" parent="@style/Theme.Cobalt">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/cobalt_primary_blue</item>
        <item name="colorPrimaryDark">@color/cobalt_primary_blue_dark</item>
    </style>

    <!-- Plugin theme applied to the application or activity -->
    <style name="PluginsTheme" parent="AppTheme">
        <item name="colorPrimary">@color/cobalt_primary_green</item>
        <item name="colorPrimaryDark">@color/cobalt_primary_green_dark</item>
    </style>

</resources>
